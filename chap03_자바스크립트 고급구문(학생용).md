# JavaScript 고급 구문

---

# 1. this 키워드

- this는 사용하는 위치에 따라 **바인딩되는 대상 객체가 달라진다.**

1. window 객체가 바인딩되는 경우 :

2. this에 DOM(Document Object Model) :

3. this에 자기가 속한 object가 바인딩되는 경우

- 실습(27_this.html)

---

# 2. 스코프(Scope)

- 변수 scope : 선언한 변수에 대한 접근성(할 수 있는 범위)을 의미한다.

1. 함수 안에서 선언 =>

2. 함수 밖에서 선언 =>

3. 함수 안과 함수 밖은 scope가 달라 let(재할당 가능)이나 const(재할당, 재선언 가능)로 동일한 이름으로 선언해도 사용이 가능하다.

- 실습 (28_scope.html)

---

# 3. 기본 함수매개변수 (default parameter)

- **실무**에서 많이 사용

- 매개변수가 존재하는 함수에서 실수로 매개변수를 전달하지 않는다면?

- 이를 예방하기 위해 전통적인 방법으로 예외처리를 한다면 다음과 같다.

- 일반적으로 함수 호출시 파라미터를 전달하지 않을 경우 undefined 값이 출력되며 이를 막기 위해 기본값을 설정할 수 있다. 이를 기본 함수매개변수(default parameter)라고 한다.

- 실습 (29_default_function_parameter)

- 실무에서 언제 유용할까? 예를 들어 정확한 날짜 포맷 입력을 요구하지만, 사용자가 정확한 포맷으로 입력하지 않는다고 가정할 때, **기본 함수매개변수**로 특정한 포맷을 입력한다면 매우 유용할 것이다.

- 실무활용예제 (20_date_object.html 의 function getIntervalDateformat 발전)

---

# 4. 나머지 매개변수(rest parameter)

- 숫자를 더하는 함수를 만든다고 가정하자.
- 그런데 더하는 숫자의 개수가 정해지지 않았다면?

- 그런데 불편하지 않을까?
- ECMA6에서 제공하는 나머지 매개변수를 이용하면 (add(2, 3, 4, 5));과 같이 배열을 이용하지 않고 간단히 처리가 가능하다.

- 실습(30_rest_parameter.html)

---

# 5. 화살표 함수

1. 함수의 표현방식 (복습) :

2. 화살표 함수의 특징
   - function 키워드 생략
   - (파라미터) 와 { }사이에 => 삽입
   - 만약 return 문장으로 이루어진 한 줄 짜리 함수라면 { } 과 return 모두 생략 가능

   - 실습(31_arrow_function.html)

   - 다음 리스트에서 1000원으로 구입가능한 음료수를 리턴하자.
     let drinkList = [
     { name: "오렌지", price: 1000 },
     { name: "파워에이드", price: 1200 },
     { name: "커피", price: 700 },
     { name: "보리음료", price: 1200 },
     { name: "코카콜라", price: 1800 },
     ];

---

# 6. 템플릿 리터럴

- 정의 : 백틱(\`)을 사용하여 문자열을 더 유연하고 읽기 쉽게 작성하는 ES6 방식

- 특징
  1.

  2.

  3.

- 실습(32_template_literals.html)

---

# 7. 스프레드 연산자

- 정의 : 배열, 객체, 문자열 등 반복 가능한(iterable) 객체의 요소를 개별적으로 펼쳐서(unpack) 나열하는 기능

- 활용
  1. 배열의 깊은 복사 :

  2. (참고)깊은 복사란? 배열의 깊은 복사(Deep Copy)는 원본 배열과 복사본 배열의 주소값이 다른 경우를 의미
  3. 여러 배열의 요소를 하나로 합치기
  4. 배열의 활용

- 실습(34_spread_operator.html)

---

# 8. _객체 구조 분해 할당(object destructuring)_

- 정의 : 객체의 속성(property)을 해체하여 개별 변수에 쉽고 간결하게 할당하는 문법

- 활용
  1. 객체의 일부분을 가져오고 싶은 경우 =>
  2. 고객의 수많은 정보 중 이름만 가져오고 싶다. 굳이 나머지 키-값을 가져올 필요가 있을까?

- 실습(35_object_destructuring.html)

---

# 9. _배열 구조 분해 할당( array destructuring)_

- 정의 : 배열의 요소를 편리하게 개별 변수로 추출하여 할당하는 ES6 문법

- 활용
  1. 의미 없는 값의 선언을 피하고자 할 경우
  2. 예를 들어 위도와 경도를 배열값으로 제공하는 오픈소스에서 값을 가져오는 함수를 작성해서 사용한다고 가정하자.

- 실습(36_array_destructuring.html)
